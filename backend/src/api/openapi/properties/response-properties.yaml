components:
  properties:
    detail:
      description: "A more detailed message."
      type: string
      example: "This is a more detailed message."

    timestamp:
      description: "The time the response was sent."
      type: string
      format: date-time

    status:
      description: "The HTTP status code."
      type: integer
      example: 200

    path:
      description: "The accessed endpoint."
      type: string
      example: "/the/accessed/path"
